#includr "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The scond number to multiply with the first number
 * Return: The restuly of multiplication between x and y
 */
int mul(int x, int y)
{
	return(x * y);
}
